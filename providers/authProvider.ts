// "use client";

// import { SessionProvider } from "next-auth/react";

// type PropsType = {
//   children?: React.ReactNode;
// };

// export const NextAuthProvider = ({ children }: PropsType) => {
//   return <SessionProvider>{children}</SessionProvider>;
// };